# Copy to .env for local development. Never commit real secrets.
PORT=3003
NODE_ENV=development

# Probe targets. Defaults resolve inside the Compose network; override these when
# running the admin service directly on the host.
# APPVIEW_HEALTH_URL=http://localhost:3001/health
# FEEDGEN_HEALTH_URL=http://localhost:3002/health
# PDS_HEALTH_URL=http://localhost:3000/xrpc/_health
# POSTGRES_HOST=localhost
# REDIS_HOST=localhost
