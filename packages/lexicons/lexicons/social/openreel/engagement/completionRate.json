{
  "lexicon": 1,
  "id": "social.openreel.engagement.completionRate",
  "defs": {
    "main": {
      "type": "object",
      "description": "Anonymized aggregate engagement for one video, computed by an indexing service from many watch events and published to feed generators. This is a derived value returned by services, not a record stored in user repositories. Lexicon has no floating-point type, so ratios are expressed in basis points (1/100 of a percent, 0-10000).",
      "required": ["subject", "totalViews", "avgCompletionRateBps", "computedAt"],
      "properties": {
        "subject": {
          "type": "string",
          "description": "AT-URI of the social.openreel.video.post the aggregate describes.",
          "format": "at-uri"
        },
        "totalViews": {
          "type": "integer",
          "description": "Number of watch events included in the aggregate. Publishers should withhold aggregates below their anonymity threshold.",
          "minimum": 0
        },
        "avgCompletionRateBps": {
          "type": "integer",
          "description": "Mean of min(watchDurationMs / videoDurationMs, 1) across included watch events, in basis points.",
          "minimum": 0,
          "maximum": 10000
        },
        "likeRatioBps": {
          "type": "integer",
          "description": "Likes divided by totalViews, in basis points.",
          "minimum": 0,
          "maximum": 10000
        },
        "computedAt": {
          "type": "string",
          "description": "When the aggregate was computed.",
          "format": "datetime"
        }
      }
    }
  }
}
